spring:
    application:
        name: zuulproxyservice

zuul:
    host:
        connect-timeout-millis: 20000
        socket-timeout-millis: 20000
    routes:
        signin:
            url: http://localhost:8081/auth/signin
            sensitiveHeaders:
        tweet:
            url: http://localhost:8082/tweet
            sensitiveHeaders:
        like:
            url: http://localhost:8082/like
            sensitiveHeaders:
        trend:
            url: http://localhost:8082/trend
            sensitiveHeaders:

server:
    port: 8080
