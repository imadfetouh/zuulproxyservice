spring:
    application:
        name: zuulproxyservice

zuul:
    host:
        connect-timeout-millis: 20000
        socket-timeout-millis: 20000
    routes:
        signin:
            url: http://$(AUTHSERVICE_HOST):8081/auth/signin
            sensitiveHeaders:
        tweet:
            url: http://$(TWEETSERVICE_HOST):8082/tweet
            sensitiveHeaders:
        like:
            url: http://$(TWEETSERVICE_HOST):8082/like
            sensitiveHeaders:
        trend:
            url: http://$(TWEETSERVICE_HOST):8082/trend
            sensitiveHeaders:

server:
    port: 8080
